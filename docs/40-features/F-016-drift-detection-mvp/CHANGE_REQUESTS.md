# Change Requests: Drift Detection MVP

No accepted change requests yet.

Scope is fixed by the P13 roadmap entry: deterministic, local, read-only drift checks against
accepted repository memory, starting with ADR reference integrity.

Any expansion beyond ADR reference integrity (for example module-ownership disagreement or semantic
contradiction detection) must be raised as a new change request or a follow-up feature.
