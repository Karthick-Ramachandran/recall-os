# Change Requests: Persist Adopt

No accepted change requests.

Richer inference (dependency graphs, module boundary detection, ownership inference) is deferred to
later features. The MVP limits inference to clear manifest and marker signals so that proposals stay
trustworthy and conservative.
