# Change Requests: MCP Capture Skill

No accepted change requests.

Server-specific capture skills (for example a Figma-only design-context skill) are out of scope; one
shared `capture-mcp-context` skill covers all MCP servers and keeps the catalog small.
