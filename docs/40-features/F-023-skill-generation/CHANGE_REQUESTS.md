# Change Requests: Skill Generation

No accepted change requests.

Claude Code-specific skill features (dynamic context injection, scripts, fork context) are
intentionally out of scope to keep generated skills portable and scriptless per ADR-0004. MCP
context capture skills are a separate follow-up feature.
