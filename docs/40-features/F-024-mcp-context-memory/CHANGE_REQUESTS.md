# Change Requests: MCP Context Memory

No accepted change requests.

A dedicated `capture-mcp-context` agent skill that routes agents to record context into the
generated MCP memory is a natural follow-up that reuses skill generation, but it is out of scope for
this MVP; the generated MCP memory carries the capture guidance inline.
