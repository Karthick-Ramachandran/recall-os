# Flutter Example

This folder shows the repository memory generated by:

```bash
recall init --preset flutter
```

The Flutter preset adds platform-aware guidance and proposed decisions. It does not create
application code or silently accept architecture choices.
