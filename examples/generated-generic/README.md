# Generic Example

This folder shows the repository memory generated by:

```bash
persist init --preset generic
```

The generic preset keeps architecture decisions neutral. It adds guidance and proposed memory
structure without selecting a framework, vendor, or application architecture.
